import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#F0604A",
      dark: "#023DA1",
    },
    secondary: {
      main: "#1A69ED",
    },
    dark: {
      primary: "#424242",
    },
  },
});
